let att=document.getElementsByClassName("cname")[0]

// console.log(att.getAttribute("id"))

// console.log(att.hasAttribute("class"))

// att.setAttribute("class","hello cname")

// att.removeAttribute("class")

// console.log(att.attributes)

// we can manually create a datta sets like a classs or with with our own ideas of name 


console.log(att.dataset)
console.log(att.dataset.hello)
console.log(att.dataset.bello)
